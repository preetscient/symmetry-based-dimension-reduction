# Batch Run Module

The `batch_run` module manages the execution of batch processing tasks. It is responsible for initializing and running the processing jobs.

## Functions

- `run_batches()`: Initializes and runs all batch processes sequentially or in parallel.

## Usage

```python
from batch_run import run_batches

run_batches()
